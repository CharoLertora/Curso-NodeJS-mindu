// argumentos de entrada
console.log(process.argv)

// controlar el proceso y su salida
// process.exit(1)

// podemos controlar eventos del proceso
// process.on('exit', () => {
//   // limpiar los recursos
// })

// current working directory
console.log(process.cwd())

// platform
console.log(process.env.PEPITO)

// Process es un objeto global que proporciona info y control sobre el proceso actual en ejecución.
// Tiene propiedades y métodos que te van a permitir interactuar con el proceso actual
